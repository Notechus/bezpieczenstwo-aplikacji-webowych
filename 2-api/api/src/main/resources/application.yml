server:
  port: 8081
logging:
  level:
    org.springframework: DEBUG

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-736968.okta.com/oauth2/default
          jwk-set-uri: https://dev-736968.okta.com/oauth2/default/v1/keys
